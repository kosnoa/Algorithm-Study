#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
typedef unsigned long long llu;

int main()
{
    ios_base::sync_with_stdio(false), cin.tie(nullptr);

    // pair<key : value>

    pair<string, int> p; 

    p = make_pair("Tony", 50);

    vector<pair<int, string>> vp;

    vp.push_back(make_pair(80, "Tony"));
    vp.push_back(make_pair(100, "Dennis"));
    vp.push_back(make_pair(70, "Alex"));
    

    return 0;
}